$(LINUX_ONLY)#!${INSTALL}/bin/${ARCH}/tcs-mk-ioc

cd ${TEST_INSTALL}

substitute "IOCPROMPT=$(SIMTOP)-sim-ioc"
substitute "SUBS=sim.subs"
substitute "TCSTOP=$(SIMTOP),TCSPV=$(SIMTCSPV),TCSSEQ=$(SIMTCSSEQ)"

epicsEnvSet("SIMTOP","$(SIMTOP)")
include "../common.src"
